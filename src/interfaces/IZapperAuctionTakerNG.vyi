# @version 0.4.3

# NG: next-gen auction-taker interface. `takeAuction` now also carries the collateral->borrow
# NG: swap (router + calldata) the taker runs in its take callback to convert the redeemed
# NG: collateral on lossy markets. The funding USDC is transferred to the taker before this call.


@external
def takeAuction(
    auction: address,
    auction_id: uint256,
    swap_router: address,
    swap_data: Bytes[10 ** 4],
):
    ...
